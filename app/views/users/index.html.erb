<div class="container">
  <div class="row d-flex justify-content-center">
    <h1 class="mt-4">Users</h1>
  </div>
  <div class="row d-flex justify-content-center">
    <div class="btn-group-vertical col-4" role="group" aria-label="Basic example">
      <% @users.each do |user| %>
        <%= link_to user.name, user_path(user), class: "btn btn-outline-success btn-block mt-2 rounded" %>
      <% end %>
    </div>
  </div>
</div>

